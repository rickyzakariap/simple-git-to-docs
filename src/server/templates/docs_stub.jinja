{% extends "base.jinja" %}
{% block content %}
<div class="w-full max-w-xl bg-white rounded-xl shadow-lg p-8 border-2 border-gray-900 text-center">
    <h2 class="text-2xl font-bold mb-4">Docs for {{ repo_name }}</h2>
    <p class="mb-6 text-gray-600">Documentation generation coming soon!</p>
    <form action="/" method="get">
        <button type="submit" class="mt-2 underline text-gray-600 hover:text-gray-900">Back to Home</button>
    </form>
</div>
{% endblock %} 